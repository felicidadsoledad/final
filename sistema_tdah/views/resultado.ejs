<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Resultado</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/ec7551d25d.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: #e8f6f3; /* Fondo en verde suave */
        }
        .container {
            margin-top: 50px;
            text-align: center;
            padding: 30px;
            background-color: #ffffff; /* Fondo blanco */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra ligera */
        }
        h1 {
            color: #1a759f; /* Azul agradable */
        }
        
        .btn-danger {
            background-color: #e63946 !important; /* Rojo fuerte */
            color: white;
            border: none;
            box-shadow: 0 4px 5px rgba(230, 57, 70, 0.5);
            padding: 10px 20px;
            margin: 10px;
            transition: transform 0.2s ease-in-out;
        }
        .btn-danger:hover {
            background-color: #c81d32 !important;
            transform: scale(1.05);
        }
        /* Botón Cancelar */
        .btn-secondary {
            background-color: #6c757d;
            color: white;
            border: none;
            box-shadow: 0 4px 5px rgba(108, 117, 125, 0.4);
            padding: 10px 20px;
            margin: 10px;
            transition: transform 0.2s ease-in-out;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
            transform: scale(1.05);
        }
        /* Botón azul: Inicio */
        a.btn-inicio {
            background-color: #0288d1; /* Azul medio */
            color: white;
            border: none;
            box-shadow: 0 4px 5px rgba(2, 136, 209, 0.4); /* Sombra azul */
        }
        a.btn-inicio:hover {
            background-color: #026aa7; /* Azul más oscuro */
            transform: scale(1.05);
            color: white;
        }
    </style>
</head>
<body>
 <!-- Generar un solo formulario tanto para registrar como para modificar y eliminar -->
<div class="container">
    <h1><%= mensaje %></h1>

    <% if (typeof rutaEliminar !== 'undefined' && rutaEliminar) { %>
        <form action="<%= rutaEliminar %>" method="POST">
            <button type="submit" class="btn btn-danger"><i class="fas fa-trash-alt" style="margin-right: 5px;"></i>Confirmar Eliminación</button>
            <a href="<%= volverA %>" class="btn btn-secondary"><i class="fas fa-undo" style="margin-right: 5px;"></i>Cancelar</a>
        </form>
    <% } else { %>
        <% if (id) { %>
            <p>ID del registro: <strong><%= id %></strong></p>
        <% } %>
        <a href="/menu" class="btn btn-inicio"><i class="fas fa-home" style="margin-right: 5px;"></i>Inicio</a>
    <% } %>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
